apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cosy-domain-provider-backend-staging
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - cosy-staging
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: management
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
