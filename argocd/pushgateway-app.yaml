apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cosy-pushgateway
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Magenta-Mause/Cosy-Domain-Provider-Deployment.git
    targetRevision: main
    path: base/pushgateway
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
